<h1><%= @patient.name %></h1>

<ul>
    <% @patient.doctors.each do |doctor| %>
        <li><%= link_to doctor.name, doctor_path(doctor) %></li>
    <% end %>
</ul>

<ul>
    <% @patient.appointments.each do |appointment| %>
        <li><%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></li>
    <% end %>

</ul>